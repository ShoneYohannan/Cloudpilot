version: '3.8'

services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://shoneyohannan_db_user:1234shone@ac-arjvt6w-shard-00-00.ug5htla.mongodb.net:27017,ac-arjvt6w-shard-00-01.ug5htla.mongodb.net:27017,ac-arjvt6w-shard-00-02.ug5htla.mongodb.net:27017/CloudPilot?ssl=true&authSource=admin&replicaSet=atlas-13ljn1-shard-0&retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=cloudpilot_secret_key
    restart: always

  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
